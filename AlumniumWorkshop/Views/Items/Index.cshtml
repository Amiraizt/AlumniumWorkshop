@using Alumnium.Core
@model IEnumerable<AlumniumWorkshop.Models.Item.ItemModel>

@{
    ViewData["Title"] = "Index";
}


<hr />

<p>
    <a class="btn btn-sm btn-primary" asp-action="Create">اضافة مادة</a>

    @if (User.IsInRole(Consts.SystemAdminUserRole) || User.IsInRole(Consts.Accountant) )
    {
        <a class="btn btn-sm btn-primary" asp-action="AddNewItemQuantity">اضافة كمية جديدة للمواد</a>
    }
</p>
<section class="content">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">المواد</h3>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <table id="datatable" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>
                                    الاسم
                                </th>
                                <th>
                                    الكمية
                                </th>
                                <th>السعر</th>
                                <th>الحالة</th>
                                <th> الاجراء</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.UnitPrice)
                                    </td>
                                    <td>
                                        @if (@item.Status == Consts.ACTIVE)
                                        {
                                            <a class="btn btn-sm btn-primary" style="background-color: green !important;" asp-action="ChangeStatus" asp-route-id="@item.Id"> مفعل </a>
                                        }
                                        else
                                        {
                                            <a class="btn btn-sm btn-danger" asp-action="ChangeStatus" asp-route-id="@item.Id"> غير مفعل</a>

                                        }

                                    </td>
                                    <td>
                                        @if (User.IsInRole(Consts.SystemAdminUserRole) || User.IsInRole(Consts.Accountant))
                                        {
                                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-icon btn-bg-light btn-active-color-primary btn-sm me-1" target="_blank" title="تعديل" data-bs-toggle="tooltip" data-bs-trigger="hover">
                                                <i class="ki-duotone ki-pencil fs-2">
                                                    <span class="path1"></span>
                                                    <span class="path2"></span>
                                                </i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@item.Id" onclick="return confirm('هل تريد الحذف ؟');" class="btn btn-icon btn-bg-light btn-active-color-primary btn-sm me-1" target="_blank" title="حذف" data-bs-toggle="tooltip" data-bs-trigger="hover">
                                                <i class="ki-duotone ki-trash fs-2" style="color:red">
                                                    <span class="path1"></span>
                                                    <span class="path2"></span>
                                                    <span class="path3"></span>
                                                    <span class="path4"></span>
                                                    <span class="path5"></span>
                                                </i>
                                            </a>
                                        }
                                     


                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>
</section>
@* @section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
} *@
</script>
