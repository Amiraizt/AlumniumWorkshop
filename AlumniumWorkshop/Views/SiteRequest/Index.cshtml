@using Alumnium.Core
@model IEnumerable<AlumniumWorkshop.Models.SiteRequest.SiteRequestModel>

@{
    ViewData["Title"] = "Index";
}


<hr />
<p>
    <a class="btn btn-sm btn-primary" asp-action="Create">اضافة موقع</a>
</p>
<section class="content">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">المواقع</h3>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <table id="datatable" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>
                                    اسم الموقع
                                </th>
                                <th>
                                    اسم صاحب الموقع
                                </th>
                                <th>رقم صاحب الموقع</th>
                                <th>عدد الامتار</th>
                                <th> الاجراء</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.SiteName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.SiteOwnerName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.SiteOwnerPhone)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.MetersNumber)
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-icon btn-bg-light btn-active-color-primary btn-sm me-1">
                                            <i class="ki-duotone ki-pencil fs-2">
                                                <span class="path1"></span>
                                                <span class="path2"></span>
                                            </i>
                                        </a>
                                         |
                                        <a asp-controller="Report" asp-action="ViewSiteReport" asp-route-id="@item.Id" class="btn btn-icon btn-bg-light btn-active-color-primary btn-sm me-1">
                                            <i class="ki-duotone ki-file fs-2">
                                                <span class="path1"></span>
                                                <span class="path2"></span>
                                            </i>
                                        </a>
                                        @* <a asp-controller="Report" asp-action="ViewSiteReport" asp-route-id="@item.Id" class="btn btn-bg-light btn-color-muted btn-active-color-primary btn-sm px-4">Report</a>
 *@
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>
</section>


